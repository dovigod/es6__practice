let saveSetting = ({notification, color:{theme}}) =>{
    console.log(theme);
    console.log(notification);
}

/*
saveSetting({
    notification:{
        alert:true,
        send:true
    },
    color:{
        theme: "red"
    }
});*/
